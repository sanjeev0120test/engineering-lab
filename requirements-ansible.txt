# Ansible controller (venv — not committed): macOS/Linux use .venv/ ; Windows WSL use .venv-wsl/
# After creating the venv and activating:
#   python -m pip install --upgrade pip
#   pip install -r requirements-ansible.txt
#   ansible-galaxy collection install -r ansible/collections.yml
ansible-core>=2.16,<2.20
ansible-lint>=26.0,<27
