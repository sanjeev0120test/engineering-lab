# Ansible stack for WSL (recommended on Windows). After: wsl
#   cd /path/to/repo && python3 -m venv .venv-wsl && source .venv-wsl/bin/activate
#   pip install -r requirements-ansible.txt
#   ansible-galaxy collection install -r ansible/collections.yml
ansible-core>=2.16,<2.20
ansible-lint>=26.0,<27
