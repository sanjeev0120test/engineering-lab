# Secrets and local env (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*_rsa
*_ed25519
id_rsa
id_ed25519
id_ecdsa
id_dsa
.aws/credentials
.aws/config
kubeconfig
*.kubeconfig
*.tfvars
!*.tfvars.example
*.tfvars.json
secrets.yaml
**/secrets/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Editors / IDEs
.idea/
.vscode/
# Cursor: real MCP config lives in the user profile only (never commit repo-local mcp.json)
.cursor/mcp.json
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
.venv-wsl/
venv/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Local shell / auth (never commit)
.netrc
.envrc
.direnv/

# Certificates / keystores
*.pfx
*.p12

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/

# Java / JVM
target/
*.class
.gradle/
build/

# Terraform (commit .terraform.lock.hcl for reproducible provider versions)
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Docker (local overrides)
docker-compose.override.yml

# Logs and temp
*.log
tmp/
temp/
.cache/

# Coverage / test artifacts
coverage/
.coverage
htmlcov/
*.cover
